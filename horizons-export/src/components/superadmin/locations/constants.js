export const DEFAULT_FORM = Object.freeze({
  label: '',
  addressQuery: '',
});

export const DEFAULT_GEOFENCE_RADIUS_METERS = 50;
export const GEOCODE_RESULTS_LIMIT = 5;
export const ADDRESS_INPUT_PLACEHOLDER = 'Ex.: Av. Paulista, 1000 - São Paulo, SP';
